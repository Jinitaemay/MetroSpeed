# 文档同步计划 (v1.0.2)

## 摘要

刷新 README.md 和 investigation_status.md，使其反映 v1.0.2 实际代码状态。当前两份文档中置信度公式和手持检测参数描述已过时（仍是本轮改动前的值）。

---

## 文件 1：README.md

### 1.1 时间线条目修正（第 232 行）

当前内容描述旧的置信度公式（0.86→0.72、10min 衰减、Cruise/Accel 奖励），需更新为实际代码：

**旧**：
```
置信度公式修复（基线 0.86→0.72、衰减满扣 4min→10min、新增 Cruise/Accel 奖励、clamp 下限 0.08→0.05）
```

**新**：
```
置信度公式重写（基线 1.0、倍率衰减模型：弯道×3/加速×2/振动×4、pureMode 锚点感知双速率、3min 锚点巡航触底、校准不重置）
```

### 1.2 时间线条目修正（手持参数）

当前内容写 80/100，实际已迭代至 40/40：

**旧**：
```
80 帧滑窗（~2s）+ 100 帧（2.5s）持续确认
```

**新**：
```
40 帧滑窗（~0.8s）+ 40 帧（~0.8s）持续确认，14 条记录零误触发
```

### 1.3 时间线条目补充

时间线 07-05 条目漏掉了：
- ZCR 计算矫正（硬编码 40Hz → 实际时间戳差值）
- 传感器状态修复（停止后不重启 bug + 启动失败后 researchSensorActive 未重置）
- 传感器名去"传感器"后缀、辅助传感器 start/stop 静默
- 文案优化（手持覆盖三行、底部说明精简）

在已有条目前加一个条目即可。

### 1.4 已知限制

现有"设备放置需尽量稳定，剧烈晃动会破坏估算" → 改为：

> 设备放置需稳定——手持姿态会被陀螺仪自动检测并中止测速

### 1.5 文件命名修改

在 项目结构 中补充两个新增工具文件：
- `tools/_confidence_analysis.py` — 置信度延迟扫描 + 状态误差分析
- `tools/_confidence_calibrate.py` — 置信度全量标定（多进程）

---

## 文件 2：investigation_status.md

### 2.1 版本抬头

v36 → v37，最后更新日期 → 2026-07-05（保持）

### 2.2 手持检测描述（第 248-253 行）

更新到实际参数：

**旧**：
```
80 帧滑窗（~2s）+ 100 帧（2.5s）持续确认 → 触发手持
```

**新**：
```
40 帧滑窗（~0.8s）+ 40 帧（~0.8s）持续确认 → 触发手持。14 条记录零误触发
```

版本号/确认帧数统统 40。

### 2.3 置信度公式描述（第 254 行）

**旧**：
```
基线 0.86→0.72，校准衰减满扣时间 4min→10min，新增 Cruise +0.10 和 Accel/Braking +0.05 奖励，clamp 下限 0.08→0.05
```

**新**：
```
重写为倍率衰减模型：基线 1.0，时间衰减×状态倍率（弯道×3/加速×2/振动×4/传导振动×1.5）+ 陀螺噪声项。pureMode 双速率（锚点 3min 触底 / 纯惯性 2min 触底）。停车校准不动置信度。22.9万帧标定验证单调性成立（10%→56.7km/h vs 90%→9.2km/h P90）
```

### 2.4 时间线条目（第 310-311 行）

同步 README 中同样的修正。

### 2.5 项目结构

tools/ 列表补充 `_confidence_analysis.py` 和 `_confidence_calibrate.py`，标注为临时诊断脚本。

### 2.6 最近完成任务列表（第 325-327 行）

更新置信度和手持条目描述到最新。

---

## 验证

- 通读 README.md，确认时间线 07-05 条目准确
- 通读 investigation_status.md v37，确认手持和置信度描述与代码一致
- git diff 仅涉及两个 .md 文件

## 不变的内容

- 算法原理、速度公式、项目结构概述不动
- 数据资产表不动
- 项目规则不碰
- 版本号不动（项目规则禁止 AI 自行升版本号）
